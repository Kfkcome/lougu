#include <bits/stdc++.h>
using namespace std;
int n;
struct elec{
    int pos;
    bool kind;//真就是正电子
};
stack<elec> left,right;//向左的和向右的栈
string position;
int main()
{
    int n;
    cin>>n>>position;
    for(int i=0;i<position.length();i++)
    {
        if(position[i]=='+'){
            
        }
    }
    return 0;
}